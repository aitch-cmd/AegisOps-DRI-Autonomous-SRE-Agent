global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "test-app"
    static_configs:
      - targets: ["host.docker.internal:80"] # Points to Kind service if using NodePort/Ingress or local port mapping
